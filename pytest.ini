[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: tests that require a running service or external runtime dependency
    manual: manual QA checks not intended for routine automated collection
